# 🚀 خطوات إنشاء قاعدة البيانات في Supabase

## 📍 الموقع الحالي للملف
الملف موجود في: `home/setup_supabase_complete.sql`

---

## ✅ الخطوات التفصيلية

### الخطوة 1: فتح Supabase Dashboard
1. اذهب إلى [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. سجّل الدخول إلى حسابك
3. اختر **مشروعك** من القائمة

### الخطوة 2: فتح SQL Editor
1. من الشريط الجانبي الأيسر، انقر على **"SQL Editor"**
2. ستفتح صفحة محرر SQL

### الخطوة 3: إنشاء استعلام جديد
1. في الجزء العلوي من منطقة المحتوى، بجوار شريط البحث **"Search queries..."**
2. انقر على زر **`+` (علامة زائد)** في الأعلى
3. من القائمة المنسدلة، اختر **"Create a new snippet"** أو **"New query"**

### الخطوة 4: فتح ملف SQL
1. افتح ملف `setup_supabase_complete.sql` من مجلد `home`
2. يمكنك فتحه بـ:
   - **VS Code**: اضغط `Ctrl+O` واختر الملف
   - **Notepad**: انقر بزر الماوس الأيمن واختر "Open with Notepad"
   - **أي محرر نصوص**: افتح الملف

### الخطوة 5: نسخ الكود
1. اضغط **`Ctrl+A`** لتحديد جميع محتويات الملف
2. اضغط **`Ctrl+C`** لنسخ الكود
3. أو انقر بزر الماوس الأيمن واختر **"Copy"**

### الخطوة 6: لصق الكود في Supabase
1. ارجع إلى Supabase SQL Editor
2. انقر داخل منطقة المحرر
3. اضغط **`Ctrl+V`** للصق الكود
4. أو انقر بزر الماوس الأيمن واختر **"Paste"**

### الخطوة 7: تشغيل الاستعلام
1. انظر إلى الأسفل في منطقة النتائج
2. انقر على زر **"Run"** (تشغيل) باللون البرتقالي على اليمين
3. أو اضغط **`Ctrl+Enter`** على لوحة المفاتيح
4. أو اضغط **`Ctrl+J`** (اختصار آخر)

### الخطوة 8: انتظار النتائج
1. انتظر حتى تظهر رسالة **"Success"** أو **"Success. No rows returned."**
2. قد تستغرق العملية **بضع دقائق** حسب حجم قاعدة البيانات
3. لا تغلق الصفحة أثناء التنفيذ

### الخطوة 9: التحقق من النتائج
1. من الشريط الجانبي الأيسر، انقر على **"Table Editor"**
2. تحقق من وجود جميع الجداول التالية:
   - ✅ `profiles`
   - ✅ `user_roles`
   - ✅ `cities`
   - ✅ `hotels`
   - ✅ `bookings`
   - ✅ `emails`
   - ✅ `email_settings`
   - ✅ `tasks`
   - ✅ `task_categories`
   - ✅ `site_settings`
   - ✅ `whatsapp_settings`
   - ✅ وغيرها...

### الخطوة 10: التحقق من RLS
1. من الشريط الجانبي، انقر على **"Authentication"** → **"Policies"**
2. تحقق من وجود سياسات RLS على الجداول
3. أو من **"Table Editor"**، انقر على أي جدول وتحقق من أن **"RLS enabled"** مفعل

---

## ⚠️ ملاحظات مهمة

### إذا ظهرت أخطاء:
1. **"relation already exists"** → هذا طبيعي، الجدول موجود بالفعل
2. **"type already exists"** → هذا طبيعي، النوع موجود بالفعل
3. **"policy already exists"** → هذا طبيعي، السياسة موجودة بالفعل

**الحل**: تجاهل هذه الأخطاء، الملف يستخدم `IF NOT EXISTS` لذلك لن يسبب مشاكل.

### إذا فشل الاستعلام:
1. تحقق من رسالة الخطأ
2. تأكد من أنك نسخت **جميع** محتويات الملف
3. تأكد من أنك لم تنسخ سطر واحد فقط
4. حاول تشغيل الاستعلام مرة أخرى

---

## ✅ بعد الانتهاء

### 1. إعادة تحميل التطبيق
1. ارجع إلى تطبيقك في المتصفح
2. اضغط **`Ctrl+Shift+R`** لإعادة التحميل الكامل
3. أو اضغط **`F5`** ثم **`Ctrl+Shift+R`**

### 2. التحقق من عمل التطبيق
1. تحقق من أن صفحة البريد تعمل
2. تحقق من أن صفحة المهام تعمل
3. تحقق من أن صفحة الحجوزات تعمل
4. تحقق من أن جميع الصفحات تعمل بشكل صحيح

---

## 📞 إذا واجهت مشاكل

### المشكلة: "Could not find the table"
**الحل**: تأكد من أنك شغّلت الاستعلام بنجاح وأن جميع الجداول موجودة في "Table Editor"

### المشكلة: "Permission denied"
**الحل**: تأكد من أنك تستخدم حساب المشرف (Admin) في Supabase

### المشكلة: الاستعلام لا ينتهي
**الحل**: 
1. انتظر بضع دقائق إضافية
2. إذا استمرت المشكلة، أعد تحميل الصفحة
3. حاول تشغيل الاستعلام مرة أخرى

---

## 🎉 تم الانتهاء!

بعد تشغيل الاستعلام بنجاح:
- ✅ جميع الجداول تم إنشاؤها
- ✅ جميع الإعدادات تم تفعيلها
- ✅ جميع السياسات تم إنشاؤها
- ✅ جميع البيانات الافتراضية تم إدراجها
- ✅ التطبيق جاهز للعمل!

---

**ملاحظة**: هذا الملف شامل ويحتوي على **جميع** الجداول والإعدادات المطلوبة. يمكنك تشغيله **مرة واحدة فقط** لإعداد قاعدة البيانات بالكامل.

