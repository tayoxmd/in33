# ✅ كيفية التشغيل الصحيحة

## ⚠️ الخطأ الشائع

**لا تضع `npm run setup-db` في SQL Editor!**

SQL Editor يتوقع **SQL فقط** وليس أوامر npm.

---

## ✅ الطريقة الصحيحة 1: استخدام Terminal

### الخطوة 1: فتح Terminal/PowerShell
1. افتح PowerShell أو Command Prompt
2. اذهب إلى مجلد `home`:
   ```powershell
   cd C:\Users\xmd55\Desktop\ithraa\home
   ```

### الخطوة 2: تعيين Connection String
```powershell
$env:DATABASE_URL = "postgresql://postgres:YOUR_PASSWORD@db.gkhgfcfdylleuweeigsn.supabase.co:5432/postgres"
```
**استبدل `YOUR_PASSWORD` بكلمة المرور الفعلية**

### الخطوة 3: تشغيل السكريبت
```powershell
npm run setup-db
```

أو مباشرة:
```powershell
node setup-database-direct.js
```

---

## ✅ الطريقة الصحيحة 2: استخدام SQL Editor

### الخطوة 1: فتح Supabase SQL Editor
1. افتح Supabase Dashboard
2. اذهب إلى **SQL Editor**
3. انقر على زر **`+`** (Create a new snippet)

### الخطوة 2: نسخ محتويات ملف SQL
1. **لا تنسخ `npm run setup-db`**
2. افتح ملف `setup_supabase_complete.sql` من مجلد `home`
3. انسخ **جميع محتوياته** (Ctrl+A ثم Ctrl+C)
4. الصق الكود في SQL Editor (Ctrl+V)

### الخطوة 3: تشغيل الاستعلام
1. انقر على زر **"Run"** أو اضغط **Ctrl+Enter**
2. انتظر رسالة **"Success"**

---

## 📝 ملخص

| المكان | ما يجب وضعه |
|--------|-------------|
| **Terminal/PowerShell** | `npm run setup-db` |
| **SQL Editor** | محتويات ملف `setup_supabase_complete.sql` |

---

## 🎯 ما يجب فعله الآن

### إذا كنت تريد استخدام Terminal:
1. افتح PowerShell
2. اذهب إلى مجلد `home`
3. تعيين `DATABASE_URL`
4. شغّل `npm run setup-db`

### إذا كنت تريد استخدام SQL Editor:
1. امسح `npm run setup-db` من SQL Editor
2. افتح ملف `setup_supabase_complete.sql`
3. انسخ جميع محتوياته
4. الصق في SQL Editor
5. انقر على "Run"

---

## ✅ النتيجة المتوقعة

بعد التشغيل الصحيح:
- ✅ رسالة "Success" أو "Success. No rows returned"
- ✅ جميع الجداول تم إنشاؤها
- ✅ جميع الإعدادات تم تفعيلها

