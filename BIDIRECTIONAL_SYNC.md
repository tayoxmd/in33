# نظام المزامنة الثنائي - Lovable ↔ Local ↔ AI

## 🎯 نظرة عامة

نظام مزامنة ثنائي الاتجاه يربط:
- **Lovable** ↔ **الملفات المحلية** ↔ **AI**
- أي تعديل في أي مكان يتم مزامنته تلقائياً في جميع الأماكن

## 🔄 كيف يعمل النظام

```
┌─────────┐         ┌──────────────┐         ┌──────┐
│ Lovable │ ←─────→ │ ملفات محلية │ ←─────→ │  AI  │
└─────────┘         └──────────────┘         └──────┘
      ↓                    ↓                    ↓
  Webhook              مراقب الملفات        API Request
```

### 1. من Lovable إلى المحلي:
- Lovable يقوم بتعديل → Webhook → تحديث الملفات المحلية → إشعار AI

### 2. من AI إلى المحلي و Lovable:
- AI يقوم بتعديل → تحديث الملفات المحلية → إرسال إلى Lovable → إشعار

### 3. من المحلي إلى Lovable:
- تعديل مباشر في الملفات → مراقب الملفات → إرسال إلى Lovable → إشعار AI

## 🚀 البدء السريع

### 1. تشغيل نظام المزامنة

```bash
npm run sync:bidirectional
```

السيرفر سيعمل على: `http://localhost:3001`

### 2. إعداد Lovable

#### في Lovable:
1. اذهب إلى **Settings** → **Webhooks**
2. أضف Webhook جديد:
   - **URL**: `http://10.88.50.181:3001/webhook`
   - **Events**: `push`, `file_changed`, `database_updated`
   - **Secret**: (اختياري)

### 3. إعداد AI

عندما أقوم بتعديل ملف، سيتم استخدام:
```javascript
await syncFilesFromAI([{
  path: 'src/App.tsx',
  content: '...',
  action: 'update'
}]);
```

## 📋 API Endpoints

### 1. Webhook من Lovable

```bash
POST http://localhost:3001/webhook

Body:
{
  "event": "push",
  "files": [
    {
      "path": "src/App.tsx",
      "content": "...",
      "action": "update"
    }
  ]
}
```

### 2. تحديث من AI

```bash
POST http://localhost:3001/update-files

Body:
{
  "files": [
    {
      "path": "src/App.tsx",
      "content": "...",
      "action": "update"
    }
  ],
  "source": "ai"
}
```

### 3. حالة المزامنة

```bash
GET http://localhost:3001/sync-status
```

### 4. سجل المزامنة

```bash
GET http://localhost:3001/sync-log?limit=50
```

### 5. تفعيل/تعطيل المزامنة

```bash
POST http://localhost:3001/sync-enable?enabled=true
```

## 🔧 استخدام من AI

عندما أقوم بتعديل ملف، سأستخدم:

```javascript
// في أي ملف AI helper
import { updateFileFromAI, createFileFromAI, deleteFileFromAI } from './ai-sync-helper.js';

// تحديث ملف
await updateFileFromAI('src/App.tsx', 'import React from "react"; ...');

// إنشاء ملف جديد
await createFileFromAI('src/components/NewComponent.tsx', '...');

// حذف ملف
await deleteFileFromAI('src/old-file.tsx');
```

## 📊 سجل المزامنة

جميع عمليات المزامنة محفوظة في `sync-log.json`:

```json
[
  {
    "action": "file_updated",
    "source": "lovable",
    "data": {
      "filePath": "src/App.tsx",
      "size": 1234
    },
    "timestamp": "2025-11-06T09:37:09.000Z"
  }
]
```

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: تعديل من Lovable

1. في Lovable، قم بتعديل `src/App.tsx`
2. Lovable يرسل Webhook → `http://10.88.50.181:3001/webhook`
3. النظام يحدث الملف المحلي
4. AI يتلقى إشعار بالتحديث

### السيناريو 2: تعديل من AI

1. AI يقوم بتعديل ملف
2. AI يرسل طلب إلى `/update-files`
3. النظام يحدث الملف المحلي
4. النظام يرسل التحديث إلى Lovable
5. Lovable يتلقى التحديث

### السيناريو 3: تعديل مباشر في الملفات

1. تعديل ملف مباشرة في `home/`
2. مراقب الملفات يكتشف التغيير
3. النظام يرسل التحديث إلى Lovable
4. AI يتلقى إشعار بالتحديث

## 🔐 الأمان

### 1. Webhook Secret (اختياري)

يمكنك إضافة secret للتحقق من Webhooks:

```javascript
const WEBHOOK_SECRET = 'your-secret-key-here';
```

### 2. IP Whitelist (يمكن إضافته)

يمكن إضافة قائمة IPs مسموحة للوصول.

## 📝 ملاحظات مهمة

1. **المزامنة ثنائية الاتجاه:** أي تعديل في أي مكان يتم مزامنته تلقائياً

2. **النسخ الاحتياطي:** يعمل بشكل مستقل ولا يتأثر بالمزامنة

3. **قاعدة البيانات:** للتحديثات في قاعدة البيانات، استخدم Webhook خاص:

```json
{
  "event": "database_update",
  "table": "users",
  "action": "insert",
  "data": { ... }
}
```

4. **المستخدمين والطلبات:** يمكن مزامنتها أيضاً عبر Webhooks

## 🛠️ استكشاف الأخطاء

### المشكلة: التحديثات لا تظهر

**الحل:**
1. تحقق من سجل المزامنة: `sync-log.json`
2. تحقق من حالة المزامنة: `GET /sync-status`
3. تأكد من أن Webhook يعمل في Lovable

### المشكلة: Lovable لا يتلقى التحديثات

**الحل:**
1. تأكد من أن Lovable API URL صحيح
2. تحقق من الاتصال بالإنترنت
3. راجع سجلات الأخطاء

### المشكلة: المزامنة لا تعمل

**الحل:**
1. تحقق من أن السيرفر يعمل: `npm run sync:bidirectional`
2. تحقق من حالة المزامنة: `GET /sync-status`
3. فعّل المزامنة: `POST /sync-enable?enabled=true`

## 🎉 جاهز!

الآن النظام مربوط بشكل كامل:
- ✅ Lovable ↔ الملفات المحلية
- ✅ AI ↔ الملفات المحلية
- ✅ الملفات المحلية → Lovable
- ✅ جميع التعديلات متزامنة تلقائياً

**ابدأ التعديل في أي مكان وشاهد المزامنة التلقائية! 🚀**




