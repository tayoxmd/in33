# إعداد ربط Lovable مع النظام المحلي

## 📡 نظرة عامة

هذا النظام يسمح لموقع Lovable بالمزامنة التلقائية مع الملفات المحلية على سطح المكتب **دون التدخل في نظام النسخ الاحتياطي**.

## 🚀 البدء السريع

### 1. تشغيل سيرفر المزامنة

```bash
npm run lovable:sync
```

السيرفر سيعمل على: `http://localhost:3001`

### 2. الحصول على عنوان IP

في Windows، افتح CMD واكتب:
```bash
ipconfig
```

ابحث عن `IPv4 Address` - هذا هو عنوان IP الخاص بك، مثال: `192.168.1.100`

### 3. إعداد Webhook في Lovable

#### الطريقة الأولى: استخدام Webhook (موصى به)

1. في Lovable، اذهب إلى **Settings** → **Webhooks**
2. أضف Webhook جديد:
   - **URL**: `http://[YOUR-IP]:3001/webhook`
   - **Events**: اختر `push` أو `file_changed`
   - **Secret**: (اختياري) يمكنك استخدام أي نص

3. عند أي تغيير في Lovable، سيتم إرسال التحديثات تلقائياً إلى النظام المحلي

#### الطريقة الثانية: استخدام API مباشر

يمكنك استخدام API مباشر من Lovable:

```javascript
// في Lovable، استخدم هذا الكود:
const response = await fetch('http://[YOUR-IP]:3001/update-files?token=YOUR_TOKEN', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    files: [
      {
        path: 'src/App.tsx',
        content: '...',
        action: 'update' // أو 'create' أو 'delete'
      }
    ]
  })
});
```

## 📋 API Endpoints

### 1. تحديث ملف واحد

```bash
POST http://localhost:3001/update-file?token=YOUR_TOKEN

Body:
{
  "path": "src/App.tsx",
  "content": "..."
}
```

### 2. تحديث عدة ملفات دفعة واحدة

```bash
POST http://localhost:3001/update-files?token=YOUR_TOKEN

Body:
{
  "files": [
    {
      "path": "src/App.tsx",
      "content": "...",
      "action": "update"
    },
    {
      "path": "src/components/Header.tsx",
      "content": "...",
      "action": "create"
    },
    {
      "path": "src/old-file.tsx",
      "action": "delete"
    }
  ]
}
```

### 3. قراءة ملف

```bash
GET http://localhost:3001/read-file?path=src/App.tsx&token=YOUR_TOKEN
```

### 4. عرض قائمة الملفات

```bash
GET http://localhost:3001/list-files?directory=src&token=YOUR_TOKEN
```

## 🔐 الأمان

- يمكنك إضافة token للتحقق من الهوية
- استخدم HTTPS في الإنتاج (يتطلب شهادة SSL)
- يمكنك إضافة IP whitelist في السيرفر

## 🔄 كيف يعمل النظام

1. **Lovable يقوم بتعديل الملفات** → يرسل Webhook أو API request
2. **السيرفر يستقبل التحديثات** → يطبقها على الملفات المحلية
3. **نظام النسخ الاحتياطي** → يراقب التغييرات بشكل مستقل ويمكنه إنشاء نسخة احتياطية
4. **لا يوجد تداخل** → المزامنة والنسخ الاحتياطي يعملان بشكل مستقل

## 📝 سجل المزامنة

جميع عمليات المزامنة يتم حفظها في: `lovable-sync-log.json`

يمكنك مراجعة السجل لمعرفة:
- ما هي الملفات التي تم تحديثها
- متى تم التحديث
- من أين جاء التحديث

## 🛠️ استكشاف الأخطاء

### المشكلة: لا يمكن الوصول من Lovable

**الحل:**
1. تأكد من أن السيرفر يعمل: `http://localhost:3001`
2. تأكد من عنوان IP الصحيح
3. تأكد من أن الجدار الناري يسمح بالاتصال على المنفذ 3001
4. جرب من نفس الشبكة (نفس WiFi)

### المشكلة: التحديثات لا تظهر

**الحل:**
1. تحقق من سجل المزامنة: `lovable-sync-log.json`
2. تأكد من أن المسار صحيح (relative to `home` directory)
3. تحقق من أن الملفات تم تحديثها فعلياً

### المشكلة: النسخ الاحتياطي لا يعمل

**الحل:**
- النسخ الاحتياطي يعمل بشكل مستقل
- تأكد من تشغيله: `npm run backup:start`
- تحقق من مجلد `backup` للنسخ الاحتياطية

## 💡 نصائح

1. **استخدم Webhook** بدلاً من API المباشر للحصول على تحديثات تلقائية
2. **اختبار أولاً**: جرب التحديث على ملف بسيط قبل التحديثات الكبيرة
3. **مراقبة السجلات**: راجع `lovable-sync-log.json` بانتظام
4. **النسخ الاحتياطي**: لا تنسى تشغيل نظام النسخ الاحتياطي التلقائي

## 🔗 أمثلة للاستخدام

### مثال 1: تحديث ملف من Lovable

```javascript
// في Lovable
const lovableAPI = async (filePath, content) => {
  const response = await fetch('http://192.168.1.100:3001/update-file', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ path: filePath, content })
  });
  return await response.json();
};

// استخدام
await lovableAPI('src/App.tsx', 'import React from "react"; ...');
```

### مثال 2: مزامنة كاملة

```javascript
// في Lovable - بعد التعديل
const syncAll = async (files) => {
  const response = await fetch('http://192.168.1.100:3001/update-files', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ files })
  });
  return await response.json();
};
```

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. راجع سجل المزامنة
2. تحقق من أن السيرفر يعمل
3. تأكد من عنوان IP والمنفذ




