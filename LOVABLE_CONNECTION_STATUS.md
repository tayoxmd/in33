# ✅ حالة الربط مع Lovable

## 📊 الحالة الحالية

### ✅ السيرفر يعمل
- **المنفذ**: `3001`
- **الحالة**: ✅ يعمل (Process ID: 23012)
- **الرابط المحلي**: `http://localhost:3001`
- **Webhook URL**: `http://10.88.50.181:3001/webhook`

## 🔗 معلومات الاتصال

### عنوان IP الحالي
```
10.88.50.181
```

### Webhook URL للاستخدام في Lovable
```
http://10.88.50.181:3001/webhook
```

## 📋 خطوات الإعداد في Lovable

### الطريقة الأولى: Webhook (موصى به)

1. **في Lovable:**
   - اذهب إلى **Settings** → **Webhooks**
   - اضغط **Add Webhook** أو **New Webhook**

2. **أدخل المعلومات:**
   - **URL**: `http://10.88.50.181:3001/webhook`
   - **Events**: اختر من القائمة:
     - ✅ `push` - عند حفظ التغييرات
     - ✅ `file_changed` - عند تغيير أي ملف
     - ✅ `database_updated` - عند تحديث قاعدة البيانات
     - ✅ `user_created` - عند إضافة مستخدم
     - ✅ `booking_created` - عند إضافة طلب
   - **Secret**: (اختياري) يمكنك تركها فارغة أو إضافة أي نص

3. **احفظ Webhook**

### الطريقة الثانية: API مباشر

إذا لم يكن Webhook متاحاً في Lovable، يمكنك استخدام API مباشر:

```javascript
// في Lovable - بعد التعديل على أي ملف
const syncToLocal = async (filePath, content) => {
  const response = await fetch('http://10.88.50.181:3001/update-file', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ path: filePath, content })
  });
  return await response.json();
};

// استخدام
await syncToLocal('src/App.tsx', 'import React from "react"; ...');
```

## 🧪 اختبار الاتصال

### 1. اختبار من المتصفح

افتح المتصفح واذهب إلى:
```
http://localhost:3001/help
```

أو من أي جهاز على نفس الشبكة:
```
http://10.88.50.181:3001/help
```

### 2. اختبار Webhook

في Lovable، قم بتعديل أي ملف بسيط، ثم تحقق من:
- ✅ الملف تم تحديثه في `C:\Users\xmd55\Desktop\ithraa\home`
- ✅ سجل المزامنة: `sync-log.json`

## 📊 API Endpoints المتاحة

### 1. Webhook من Lovable
```
POST http://10.88.50.181:3001/webhook
```

### 2. تحديث ملف واحد
```
POST http://10.88.50.181:3001/update-file
Body: { "path": "src/App.tsx", "content": "..." }
```

### 3. تحديث عدة ملفات
```
POST http://10.88.50.181:3001/update-files
Body: { "files": [{ "path": "...", "content": "...", "action": "update" }] }
```

### 4. مزامنة قاعدة البيانات
```
POST http://10.88.50.181:3001/database-sync
Body: { "operation": { "type": "add_user", "table": "users", "data": {...} } }
```

### 5. حالة المزامنة
```
GET http://10.88.50.181:3001/sync-status
```

### 6. سجل المزامنة
```
GET http://10.88.50.181:3001/sync-log?limit=50
```

## 🔄 كيف يعمل النظام

### من Lovable إلى المحلي:
1. تعديل في Lovable → Webhook → `http://10.88.50.181:3001/webhook`
2. النظام يستقبل التحديث → يحدث الملف المحلي
3. AI يتلقى إشعار بالتحديث

### من AI إلى Lovable:
1. AI يقوم بتعديل → تحديث الملف المحلي
2. إرسال إلى Lovable → Lovable يتلقى التحديث

### من المحلي إلى Lovable:
1. تعديل مباشر في الملفات → مراقب الملفات يكتشف
2. إرسال إلى Lovable → Lovable يتلقى التحديث

## 🛠️ استكشاف الأخطاء

### المشكلة: لا يمكن الوصول من Lovable

**الحل:**
1. ✅ تأكد من أن السيرفر يعمل: `npm run sync:bidirectional`
2. ✅ تأكد من عنوان IP الصحيح: `10.88.50.181`
3. ⚠️ تأكد من أن Lovable والسيرفر على نفس الشبكة
4. ⚠️ تحقق من الجدار الناري (Firewall) - قد تحتاج إلى السماح بالمنفذ 3001

**لإضافة استثناء في Firewall:**
```powershell
# في PowerShell (كمسؤول)
New-NetFirewallRule -DisplayName "Lovable Sync" -Direction Inbound -LocalPort 3001 -Protocol TCP -Action Allow
```

### المشكلة: التحديثات لا تظهر

**الحل:**
1. تحقق من سجل المزامنة: `sync-log.json`
2. تأكد من أن المسار صحيح (relative to `home` directory)
3. مثال: ملف `src/App.tsx` في Lovable يجب أن يكون `src/App.tsx` في الطلب

### المشكلة: Webhook لا يعمل

**الحل:**
1. استخدم API مباشر بدلاً من Webhook
2. تحقق من أن URL صحيح: `http://10.88.50.181:3001/webhook`
3. تأكد من أن Lovable يرسل البيانات بصيغة JSON

## 📝 ملاحظات مهمة

1. **المزامنة ثنائية الاتجاه:** أي تعديل في أي مكان يتم مزامنته تلقائياً
2. **النسخ الاحتياطي مستقل:** يعمل بشكل تلقائي ولا يتأثر بالمزامنة
3. **السجلات:** جميع عمليات المزامنة محفوظة في `sync-log.json`

## 🎯 الخطوات التالية

1. ✅ السيرفر يعمل
2. ⏳ إعداد Webhook في Lovable
3. ⏳ اختبار المزامنة
4. ⏳ التحقق من السجلات

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. راجع سجل المزامنة: `sync-log.json`
2. تحقق من حالة المزامنة: `GET /sync-status`
3. راجع ملفات الإعداد: `LOVABLE_SETUP_QUICK.md`, `FINAL_SYNC_SETUP.md`

