# دليل البدء السريع - Ithraa System

## ✅ تم إصلاح المشكلة!

تم تثبيت جميع التبعيات بنجاح. الآن يمكنك استخدام جميع الأوامر.

## 🚀 الأوامر الأساسية

### 1. النسخ الاحتياطي

```bash
# إنشاء نسخة احتياطية واحدة
npm run backup:once

# بدء نظام النسخ الاحتياطي التلقائي (كل 20 دقيقة)
npm run backup:start
```

### 2. استعادة نسخة احتياطية

```bash
npm run restore backup_2025-11-06_09-37-09
```

### 3. تشغيل المشروع على localhost

```bash
npm run dev
```

المشروع سيعمل على: `http://localhost:8080`

### 4. نظام إدارة الأوامر

```bash
npm run server
```

السيرفر سيعمل على: `http://localhost:3000`

### 5. نظام مزامنة Lovable

```bash
npm run lovable:sync
```

السيرفر سيعمل على: `http://localhost:3001`

### 6. ربط cPanel

```bash
# رفع الملفات مباشرة
npm run cpanel:upload upload-files

# رفع نسخة احتياطية
npm run cpanel:upload upload-backup <path-to-backup.zip>
```

## 📝 ملاحظات مهمة

1. **تأكد من أنك في المجلد الصحيح:**
   ```bash
   cd C:\Users\xmd55\Desktop\ithraa
   ```

2. **إذا واجهت خطأ "package.json not found":**
   - تأكد من أنك في المجلد الصحيح
   - قم بتشغيل: `npm install`

3. **للاستخدام من PowerShell:**
   - استخدم `;` بدلاً من `&&` لفصل الأوامر
   - مثال: `cd C:\Users\xmd55\Desktop\ithraa; npm run backup:once`

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: إنشاء نسخة احتياطية

```bash
cd C:\Users\xmd55\Desktop\ithraa
npm run backup:once
```

### السيناريو 2: تشغيل المشروع

```bash
cd C:\Users\xmd55\Desktop\ithraa
npm run dev
```

ثم افتح: `http://localhost:8080`

### السيناريو 3: ربط Lovable

```bash
cd C:\Users\xmd55\Desktop\ithraa
npm run lovable:sync
```

ثم في Lovable، أضف Webhook:
- URL: `http://10.88.50.181:3001/webhook`

### السيناريو 4: بدء النسخ الاحتياطي التلقائي

```bash
cd C:\Users\xmd55\Desktop\ithraa
npm run backup:start
```

سيبدأ النظام بمراقبة التغييرات وإنشاء نسخ احتياطية كل 20 دقيقة.

## 🔧 حل المشاكل

### المشكلة: "Cannot find package"

**الحل:**
```bash
cd C:\Users\xmd55\Desktop\ithraa
npm install
```

### المشكلة: "package.json not found"

**الحل:**
- تأكد من أنك في المجلد الصحيح: `C:\Users\xmd55\Desktop\ithraa`
- استخدم: `cd C:\Users\xmd55\Desktop\ithraa`

### المشكلة: "command not recognized"

**الحل:**
- في PowerShell، استخدم `;` بدلاً من `&&`
- مثال: `cd C:\Users\xmd55\Desktop\ithraa; npm run backup:once`

## 📚 الملفات المرجعية

- `README.md` - دليل شامل
- `LOVABLE_SETUP_QUICK.md` - إعداد Lovable السريع
- `lovable-webhook-setup.md` - دليل إعداد Lovable المفصل

## ✅ جاهز للاستخدام!

الآن جميع الأوامر تعمل بشكل صحيح. يمكنك البدء في استخدام النظام.




