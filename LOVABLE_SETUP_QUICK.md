# إعداد سريع - ربط Lovable ✅

## 🎯 الخطوات السريعة

### 1. تأكد من أن السيرفر يعمل

```bash
npm run lovable:sync
```

يجب أن ترى:
```
=== نظام مزامنة Lovable يعمل الآن ===
الرابط المحلي: http://localhost:3001
Webhook URL: http://localhost:3001/webhook
```

### 2. عنوان IP الخاص بك

**عنوان IP الحالي:** `10.88.50.181`

**Webhook URL للاستخدام:**
```
http://10.88.50.181:3001/webhook
```

### 3. إعداد Lovable

#### الطريقة الأولى: Webhook (موصى به)

1. في Lovable، اذهب إلى **Settings** → **Webhooks**
2. اضغط **Add Webhook**
3. أدخل:
   - **URL**: `http://10.88.50.181:3001/webhook`
   - **Events**: اختر `push` أو `file_changed`
4. احفظ

**الآن:** عند أي تعديل في Lovable، سيتم تطبيقه تلقائياً على الملفات المحلية!

#### الطريقة الثانية: API مباشر

إذا لم يكن Webhook متاحاً في Lovable، يمكنك استخدام API مباشر:

```javascript
// في Lovable - بعد التعديل على أي ملف
const syncToLocal = async (filePath, content) => {
  const response = await fetch('http://10.88.50.181:3001/update-file', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ path: filePath, content })
  });
  return await response.json();
};

// استخدام
await syncToLocal('src/App.tsx', 'import React from "react"; ...');
```

### 4. اختبار الاتصال

افتح المتصفح واذهب إلى:
```
http://localhost:3001/help
```

أو من أي جهاز على نفس الشبكة:
```
http://10.88.50.181:3001/help
```

### 5. اختبار المزامنة

في Lovable، قم بتعديل أي ملف بسيط، ثم تحقق من:
- ✅ الملف تم تحديثه في `C:\Users\xmd55\Desktop\ithraa\home`
- ✅ سجل المزامنة: `lovable-sync-log.json`

## 🔍 التحقق من المزامنة

### 1. سجل المزامنة

افتح ملف: `lovable-sync-log.json`

ستجد جميع عمليات المزامنة:
- الملفات التي تم تحديثها
- الوقت والتاريخ
- النتيجة

### 2. التحقق من الملفات

افتح مجلد `home` وتحقق من أن الملفات تم تحديثها.

### 3. النسخ الاحتياطي

**⚠️ مهم:** نظام النسخ الاحتياطي يعمل بشكل مستقل!

- ✅ عندما تقوم بتعديل ملف من Lovable → يتم تحديثه محلياً
- ✅ نظام النسخ الاحتياطي يراقب التغييرات → ينشئ نسخة احتياطية بعد 20 دقيقة
- ✅ لا يوجد تداخل بين النظامين

## 🛠️ استكشاف الأخطاء

### المشكلة: لا يمكن الوصول من Lovable

**الحل:**
1. تأكد من أن السيرفر يعمل: `npm run lovable:sync`
2. تأكد من عنوان IP الصحيح: `10.88.50.181`
3. تأكد من أن Lovable والسيرفر على نفس الشبكة
4. تحقق من الجدار الناري (Firewall) - قد تحتاج إلى السماح بالمنفذ 3001

### المشكلة: التحديثات لا تظهر

**الحل:**
1. تحقق من سجل المزامنة: `lovable-sync-log.json`
2. تأكد من أن المسار صحيح (relative to `home` directory)
3. مثال: ملف `src/App.tsx` في Lovable يجب أن يكون `src/App.tsx` في الطلب

### المشكلة: Webhook لا يعمل

**الحل:**
1. استخدم API مباشر بدلاً من Webhook
2. تحقق من أن URL صحيح: `http://10.88.50.181:3001/webhook`
3. تأكد من أن Lovable يرسل البيانات بصيغة JSON

## 📝 ملاحظات مهمة

1. **المزامنة أحادية الاتجاه:** من Lovable إلى الملفات المحلية فقط
2. **النسخ الاحتياطي مستقل:** يعمل بشكل تلقائي ولا يتأثر بالمزامنة
3. **السجلات:** جميع عمليات المزامنة محفوظة في `lovable-sync-log.json`

## 🎉 جاهز!

الآن النظام مربوط بـ Lovable:
- ✅ أي تعديل في Lovable → يطبق على الملفات المحلية
- ✅ النسخ الاحتياطي يعمل بشكل مستقل
- ✅ لا يوجد تداخل بين النظامين

**ابدأ التعديل في Lovable وشاهد الملفات تتحدث تلقائياً! 🚀**




